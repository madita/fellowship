import { describe, it, expect } from 'vitest';

describe('Basic Test Example', () => {
    it('confirms that true is true', () => {
        expect(true).toBe(true);
    });

    it('confirms that 1 + 1 = 2', () => {
        expect(1 + 1).toBe(2);
    });
});
